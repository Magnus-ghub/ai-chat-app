services:
  ai-chat-app:
    build: .
    container_name: ai-chat-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    env_file:
      - .env.local